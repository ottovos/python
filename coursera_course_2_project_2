#the function strip punctuation. Basically you loop through the punctation charachter list (for punct_char in punctuation_chars)and check if the punctuation is present in the word if punc in word:

punctuation_chars = ["'", '"', ",", ".", "!", ":", ";", '#', '@']


def strip_punctuation(word):
    for punct_char in punctuation_chars:
        if punct_char in word:
            word = word.replace(punct_char, "")
    return word

print(strip_punctuation('#Amazing'))
print(strip_punctuation('wonderful'))
print(strip_punctuation('wow'))
print(strip_punctuation('incredible'))
